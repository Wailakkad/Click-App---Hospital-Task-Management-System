export default function ResetPasswordPage() {
    return (
        <div>
        <h1>Reset Password Page</h1>
        <p>This is where users can reset their password using the token provided in the URL.</p>
        </div>
    );
}